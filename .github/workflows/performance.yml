# name: Performance Tests

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main ]
#   workflow_dispatch:  # Allow manual runs

# jobs:
#   performance:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         python-version: ['3.8', '3.9', '3.10', '3.11']

#     steps:
#     - uses: actions/checkout@v4

#     - name: Set up Python ${{ matrix.python-version }}
#       uses: actions/setup-python@v4
#       with:
#         python-version: ${{ matrix.python-version }}

#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install pytest httpx pytest-asyncio uvicorn
#         pip install -e .

#     - name: Run performance tests
#       run: |
#         python -m pytest tests/test_performance.py -v --tb=short

#     - name: Run performance demo
#       run: |
#         timeout 30s python demo_performance.py || true

#     - name: Generate performance report
#       run: |
#         python run_performance_tests.py || true

#     - name: Upload performance results
#       uses: actions/upload-artifact@v4
#       if: always()
#       with:
#         name: performance-results-py${{ matrix.python-version }}
#         path: |
#           performance_results.json
#           performance_section.md
#         retention-days: 7